# SatyamOS v5.0

Project type: Next.js App Router portfolio.

Stack: TypeScript, Tailwind CSS, Framer Motion, GSAP, Three.js, Lenis, React Icons.

Workspace layout:
- `src/app` for routes, metadata, and global styles
- `src/components` for the OS shell and supporting visuals
- `src/constants` for portfolio content
- `src/lib` for utilities
- `src/types` for shared types

Current status:
- Project scaffolded in the workspace root.
- Production build passes with `npm run build`.
- README is present and aligned with the project.

Run commands:
- `npm run dev`
- `npm run build`

Notes:
- Replace placeholder social links and resume artifact before publishing.
- Keep edits focused on the story-driven OS portfolio experience.
